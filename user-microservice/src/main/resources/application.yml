jwt:
  secret: REMOVED_JWT_SECRET
  expiration: 3600000

server:
  port: 8081
  servlet:
    context-path: /api/v1

spring:
  application:
    name: user-microservice
    version: 1.0.0

  datasource:
    url: jdbc:mysql://localhost/users
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update

  security:
    user:
      name: ""
      password: ""

appdescription: API para gestionar usuarios
appversion: 1.0.0